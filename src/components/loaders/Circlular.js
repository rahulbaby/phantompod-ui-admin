import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

export default () => (
  <div style={{ paddingTop: '30vh', textAlign: 'center', width: '100%' }}>
    <CircularProgress />
  </div>
);
